# Face-Recognition-System-With-Greetings
Required files to run the code 
Installation required in terminal
✅ Modules to Install:
Module                       Install Commands               Notes 
opencv-contrib-python        pip install opencv-            Needed for cv2.face.LBPHFaceRecognizer_create()
                             contrib-python
numpy                        pip install numpy              For array and label handling
gTTS                         pip install gTTS               Google Text-to-Speech
pygame                       pip install pygame             For playing audio greetings

🔄 Final Installation Command (All in One):
pip install opencv-contrib-python numpy gTTS pygame
